name: starter_project
description: "A new Flutter project."


version: 1.0.0+1

environment:
  sdk: '>=3.2.2 <4.0.0'

dependencies:
  flutter_localization: ^0.2.0   # for multiple languages
  flutter_bloc: ^8.1.3    # State management
  get_it: ^7.6.4     # Dependency injection
  go_router: ^12.1.1 # Navigator 2 API 
  flutter:
    sdk: flutter

 
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0


flutter:

 
  uses-material-design: true
  assets:
    - assets/images/
 
  # fonts:
  #   - family: Cairo-VariableFont_slnt,wght.ttf
  #     fonts:
  #       - asset: assets/fonts/Cairo-VariableFont_slnt,wght.ttf
 

flutter_intl:
  enabled: true
